/* AMAYA OS 2014 */
/* GNU GPL */
#include <TerminalCodes.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <files.h>
#include <debug.h>
#include <errno.h>
#include <dirent.h>
#include <sys/utsname.h>
#include <sys/wait.h>
#include <fcntl.h>

using namespace debug;

int main(int argc, char **argv)
{
    out() << "#########################################################################" << endl;
    error() << "# AMAYA OS MINI-DESKTOP v0.02                                           #" << endl;
    out() << "#########################################################################" << endl;
    printf("# AMAYA OS MINI-DESKTOP It's free software and it's running on 0.05     #\r\n");
    out() << "# Iconos/Software de escritorio :                                       #" << endl;
    error() << "# Amaya OS DIRectory Manager - adir (v0.02)                             #" << endl;
    error() << "# Amaya OS X PS - axps (v0.01)                                          #" << endl;
    error() << "# Salir del modo grafico (X) - sh                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#                                                                       #" << endl;
    error() << "#########################################################################" << endl;
    printf("Introducce comando grafico : ");
    /* Exit immediately. */
    return EXIT_SUCCESS;
}
